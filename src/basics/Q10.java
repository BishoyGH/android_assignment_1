package basics;
/*
    6-what is the value of x,y,z and w
    int x=3;
    int y=2;
    int z=X++;
    int w=++Y;
 */
public class Q10 {
    public static void main(String[] args) {
        // I will assume that is z = x++ (in lowercase) otherwise it will give me an error
        // I will assume that is y = y++ (in lowercase) otherwise it will give me an error

        // x = 4    -> incremented by one in third line
        // y = 3    -> incremented by one in fourth line
        // z = 3    -> post increment (increment happens after assignment)
        // w = 3    -> pre increment (increment happens first then assignment)
    }
}
